<!--
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Dashboard</title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>

  <h1>Welcome, <%= user.username %></h1>

  <h2>Your Agencies</h2>

  <ul>
    
    <% if (user.agencies && user.agencies.length > 0) { %>

      <% user.agencies.forEach((agency) { %>

        <li>

          <h3><%= agency.name %></h3>

          <p>Profile Login URL: <%= agency.profileLoginUrl %></p>

          <p>Agent Name: <%= agency.agentName %></p>

          <p>Agent Email: <%= agency.agentEmail %></p>

          <p>Agent Tel No: <%= agency.agentTelNo %></p>

          <p>Street Address: <%= agency.streetAddress %></p>

          <p>City: <%= agency.city %></p>

          <p>Country: <%= agency.country %></p>

          <p>Contact Number: <%= agency.contactNumber %></p>

          <p>Operating Hours: <%= agency.operatingHours %></p>

          <h4>Jobs:</h4>

          <ul>
            <% if (agency.jobs && agency.jobs.length > 0) { %>

              <% agency.jobs.forEach((job) { %>

                <li><%= job.title %> - <%= job.status %></li>

              <% }) %>

            <% } else { %>

              <li>No jobs found.</li>

            <% } %>

          </ul>

        </li>

      <% }) %>

    <% } else { %>
      
      <li>No agencies found.</li>

    <% } %>

  </ul>

  <h2>Your Platforms</h2>

  <ul>
    <% if (user.platforms && user.platforms.length > 0) { %>

      <% user.platforms.forEach((platform) { %>

        <li>

          <h3><%= platform.name %></h3>

          <p>Profile Login URL: <%= platform.profileLoginUrl %></p>

          <p>Contact Number: <%= platform.contactNumber %></p>

          <h4>Jobs:</h4>

          <ul>

            <% if (platform.jobs && platform.jobs.length > 0) { %>

              <% platform.jobs.forEach((job) { %>

                <li><%= job.title %> - <%= job.status %></li>

              <% }) %>

            <% } else { %>

              <li>No jobs found.</li>

            <% } %>

          </ul>

        </li>

      <% }) %>

    <% } else { %>

      <li>No platforms found.</li>

    <% } %>

  </ul>

</body>

</html>
-->
